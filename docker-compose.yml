services:
  backend:
    build: ./src/backend
    ports:
      - "8000:8000"
    env_file:
      - ./src/backend/.env
    restart: always

  frontend:
    build: ./src/frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
    restart: always
